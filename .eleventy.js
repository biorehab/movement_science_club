module.exports = function(eleventyConfig) {
    return {
      dir: {
        input: "src",  // Where our content is
        output: "_site" // Where Eleventy will generate the site
      }
    };
  };